# pubmed

# required packages 

```pip install biopython```

# Pipelines
## Query (Disease Name) AND (GENE NAME)

```bash query_gene_AND_disease.sh```

You can change the disease name in the bash script and the gene list in genelist.txt.